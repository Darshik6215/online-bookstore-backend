export enum COMMON_ROUTE {
  auth = "/api/user",
  book = "/api/book",
}

export enum USER_ROUTE {
  register = "/register",
  login = "/login",
}

export enum BOOK_ROUTE {
  create = "/create",
  all = "/all",
}
